# Public Assets Folder

Files here are served from the site root. The audio engine checks for custom jump/splash sounds in this priority order and falls back to its synthesized sound if none are found:

1. `jump.mp3`
2. `jump.wav`
3. `splash.mp3`
4. `splash.wav`
